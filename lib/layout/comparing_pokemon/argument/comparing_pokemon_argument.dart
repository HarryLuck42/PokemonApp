import '../../../model/response/pokemon_response.dart';

class ComparingPokemonArgument{
  final Pokemon first;
  final Pokemon second;
  const ComparingPokemonArgument({required this.first, required this.second});
}